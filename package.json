{
  "name": "game-data-api-netlify",
  "version": "1.0.0",
  "description": "API para extrair dados de jogos para deploy no Netlify",
  "scripts": {
    "start": "netlify dev"
  },
  "dependencies": {
    "express": "^4.18.2",
    "serverless-http": "^3.2.0"
  },
  "devDependencies": {
    "netlify-cli": "^17.10.1"
  }
}
